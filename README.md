<h1 align="center">🧠 AI Travel Planner</h1> <p align="center"> An <strong>LLM-powered travel planning agent</strong> that generates personalized itineraries based on your destination, budget, dates, and interests. </p> <p align="center"> <code>Python</code> · <code>LLM / Generative AI</code> · <code>Modular Architecture</code> </p> <hr /> <h2>🚀 Overview</h2> <p> <strong>AI Travel Planner</strong> is a backend-focused project that encapsulates an AI-powered travel agent in a clean Python package. The core logic lives inside the <code>src/</code> directory and is structured into: </p> <ul> <li><strong>Chains</strong> – Orchestrate LLM prompts and responses (itinerary generation logic).</li> <li><strong>Core</strong> – The main planner/agent responsible for building itineraries.</li> <li><strong>Config</strong> – Centralized configuration and environment settings.</li> <li><strong>Utils</strong> – Logging and custom exception handling.</li> </ul> <p> The <code>app.py</code> file at the repository root serves as the main entrypoint (for running the planner as a script or exposing it via an API, depending on how you wire it up). </p> <hr /> <h2>✨ Features</h2> <ul> <li><strong>LLM-powered itinerary generation</strong> – Design to generate multi-day itineraries from high-level user input.</li> <li><strong>Modular codebase</strong> – Clear separation between chains, planner logic, config, and utilities.</li> <li><strong>Config-driven setup</strong> – Centralized configuration via <code>src/config/config.py</code> (and optionally a <code>.env</code> file).</li> <li><strong>Structured logging</strong> – Custom logger for easier debugging and observability.</li> <li><strong>Packaged as a library</strong> – Includes <code>setup.py</code> and <code>AI_TRAVEL_AGENT.egg-info</code> for packaging.</li> </ul> <hr /> <h2>📁 Project Structure</h2> <pre><code>AI_TRAVEL_PLANNER/ ├── app.py ├── requirements.txt ├── setup.py ├── .gitignore ├── AI_TRAVEL_AGENT.egg-info/ │ └── ... (package metadata) └── src/ ├── __init__.py ├── chains/ │ ├── __init__.py │ └── itinerary_chain.py ├── core/ │ ├── __init__.py │ └── planner.py ├── config/ │ ├── __init__.py │ └── config.py └── utils/ ├── __init__.py ├── custom_exception.py └── logger.py </code></pre> <hr /> <h2>🧱 Tech Stack</h2> <ul> <li><strong>Language:</strong> Python</li> <li><strong>Core:</strong> LLM-powered chains and planner logic</li> <li><strong>Packaging:</strong> <code>setup.py</code>, <code>AI_TRAVEL_AGENT.egg-info</code></li> <li><strong>Logging &amp; Errors:</strong> Custom logger + exception classes</li> </ul> <hr /> <h2>📦 Installation</h2> <h3>1️⃣ Clone the repository</h3> <pre><code>git clone https://github.com/omprakash0702/AI_TRAVEL_PLANNER.git cd AI_TRAVEL_PLANNER </code></pre> <h3>2️⃣ Create &amp; activate a virtual environment (recommended)</h3> <pre><code>python -m venv venv venv\Scripts\activate # On Windows # source venv/bin/activate # On macOS / Linux </code></pre> <h3>3️⃣ Install dependencies</h3> <pre><code>pip install -r requirements.txt </code></pre> <hr /> <h2>⚙️ Configuration</h2> <p> Configuration for the travel planner lives in <code>src/config/config.py</code>. You can adapt it to read from environment variables or a <code>.env</code> file. </p> <p> Typical configuration values may include (these are examples, adjust to match your actual code): </p> <pre><code># Example idea for config.py (simplified) LLM_PROVIDER = "openai" # or any provider you use LLM_MODEL_NAME = "gpt-4o" # example model name DEFAULT_DAYS = 3 DEFAULT_BUDGET = "medium" </code></pre> <p> If you are using secrets (API keys), keep them out of the code and load them via environment variables. </p> <hr /> <h2>🏃 Usage</h2> <h3>Option 1 – Run the app entrypoint</h3> <p> If <code>app.py</code> is wired as a CLI or API server, you can start it with: </p> <pre><code>python app.py </code></pre> <p> Refer to <code>app.py</code> for the exact interface (e.g., FastAPI / Flask app, CLI, etc.). </p> <h3>Option 2 – Use the planner directly in Python</h3> <p> You can also import the planner from the <code>src/core/planner.py</code> module and use it inside your own scripts or notebooks. </p> <pre><code>from src.core.planner import TravelPlanner # adjust the class name to match your implementation planner = TravelPlanner() itinerary = planner.plan_trip( destination="Goa", days=3, budget="medium", interests=["beach", "food", "nightlife"], ) print(itinerary) </code></pre> <p> Adjust the class name and method signature to match what you defined in <code>planner.py</code>. </p> <hr /> <h2>🧪 Development Notes</h2> <ul> <li>Keep your virtual environment out of Git by using <code>.gitignore</code> (already configured).</li> <li>Extend <code>src/chains/itinerary_chain.py</code> if you want more advanced prompt logic or multi-step reasoning.</li> <li>Use <code>src/utils/logger.py</code> for consistent logging across modules.</li> <li>Use <code>src/utils/custom_exception.py</code> to define and raise structured, meaningful errors.</li> </ul> <hr /> <h2>📝 Roadmap Ideas</h2> <ul> <li>🌍 Support for multi-city itineraries</li> <li>📅 Integration with live flight/hotel APIs</li> <li>🗺️ Map / distance awareness (travel time between POIs)</li> <li>💾 Persistence layer to save and reload itineraries</li> <li>🌐 Simple frontend or API client (Streamlit / React / FastAPI docs)</li> </ul> <hr /> <h2>🤝 Contributing</h2> <p> This is a personal/portfolio project. Feel free to fork the repository, open issues, or adapt the architecture for your own AI travel planning experiments. </p> <hr /></p> <img width="460" height="458" alt="image" src="https://github.com/user-attachments/assets/a472e0b8-9adf-4fc3-916d-375122a049d7" />

